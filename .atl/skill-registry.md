# Clyde — Skill Registry

Auto-generated by `/sdd-init` on 2026-04-30. Refresh via `update-skills` or `/sdd-init`.

## Project Conventions (Compact Rules — auto-injected into sub-agents)

- **Stack**: Go 1.26.2 + Bubble Tea v2 (planned). Build: `go build ./cmd/clyde`. Run binary: `clyde`.
- **Architecture**: Hexagonal + DDD.
  - `internal/domain/` — pure, stdlib-only, no I/O imports.
  - `internal/application/` — use cases composing ports.
  - `internal/ports/` — interfaces (the seams: `SessionSource`, `TodoSource`, `Clock`, etc.).
  - `internal/adapters/*` — concrete implementations (jsonl, todofile, git, tui).
  - `cmd/clyde/main.go` — composition root, wires everything.
- **Domain ubiquitous language**: Session, Turn, Event, ToolCall, Subagent, Usage, Project, Todo. Misnamed concepts get rejected in review.
- **Hexagonal discipline**: do NOT wrap stdlib for the sake of it. Wrap I/O when it has SEMANTICS (a JSONL stream is a domain concept; a generic file is not).
- **TDD**: Strict mode enabled. Failing test precedes implementation in domain/application/parser layers. teatest for TUI flow tests.
- **Style**: gofmt + go vet (built-in) + golangci-lint v2.11.4 (`.golangci.yml` enforces hexagonal layering via depguard).
- **Commits**: Conventional commits only. No "Co-Authored-By" or AI attribution. (Per `~/.claude/CLAUDE.md`.)
- **Tools (per `~/.claude/CLAUDE.md`)**: use `eza`, `bat`, `rg`, `fd`, `sd` instead of `ls`, `cat`, `grep`, `find`, `sed`. Never build after changes (CI handles that).

## User Skills

### go-testing — Triggered when writing Go tests, using teatest, or adding coverage

**Compact rules**:
- **Table-driven tests** are the default for domain/application logic. Struct: `name, input, expected, wantErr`.
- For `bubbletea.Model` state transitions, test `Update()` directly — no teatest needed for unit tests.
- For end-to-end TUI flows: `teatest.NewTestModel(t, m)`, send `tea.KeyMsg`, `tm.WaitFinished(t, teatest.WithDuration(time.Second))`, assert on `tm.FinalModel(t)`.
- **Golden file tests** for visual output: `testdata/<TestName>.golden`. Support `-update` flag.
- `t.TempDir()` for filesystem tests (fsnotify adapter, JSONL fixtures).
- Mock via interfaces (ports), not package-level globals.

**Commands**: `go test ./...`, `go test -cover ./...`, `go test -run TestX`, `go test -update ./...`, `go test -short ./...`

**Source**: `~/.claude/skills/go-testing/SKILL.md`

### branch-pr — Triggered when creating PRs

**Compact rules**: Issue-first enforcement. PR title under 70 chars. Conventional commit prefixes in title. Scope by domain layer when relevant. Include test plan checklist in body.

**Source**: `~/.claude/skills/branch-pr/SKILL.md`

### issue-creation — Triggered when filing issues

**Compact rules**: Issue-first workflow. Bug reports include reproduction steps. Feature requests include user value statement and link to the relevant spec in `openspec/`.

**Source**: `~/.claude/skills/issue-creation/SKILL.md`

### judgment-day — Adversarial dual-review (on demand only)

Trigger via explicit user phrase ("judgment day", "dual review", "juzgar"). Not used automatically.

**Source**: `~/.claude/skills/judgment-day/SKILL.md`

### finalize-project / update-finalize — Deferred to 1.0 milestone

Not in scope until clyde is preparing for delivery.

## Skills NOT Applicable

- **frontend-design / superdesign** — TUI project, not web UI.
- **skill-creator** — clyde is not a skill development project.
- **find-skills** — meta-tool, on demand only.

## Persistence Strategy

- **Artifact store**: openspec (file-based, committable). All specs/design/tasks live under `openspec/`.
- **Engram**: milestone-only saves (per user preference). Triggered after each big change archives, on key architectural decisions, and on bug-fix-with-non-obvious-root-cause.
- **Skill registry**: this file is mode-independent infrastructure; mirrored to engram for orchestrator skill resolution.

## Source files referenced

- `~/.claude/CLAUDE.md` (global conventions: commits, tools, language, skill auto-load triggers)
- `~/.claude/skills/go-testing/SKILL.md`
- `~/.claude/skills/branch-pr/SKILL.md`
- `~/.claude/skills/issue-creation/SKILL.md`
- `~/.claude/skills/judgment-day/SKILL.md`
- `openspec/config.yaml` (project-level SDD config)
